---
description: Run the monthly Cristo witness team for the dated month folder.
agent: orchestrator
subtask: true
---

Run the monthly witness team via @orchestrator for month $ARGUMENTS (default current YYYY-MM).

Steps: read AGENTS.md, MEMORY.md, BOARD.md To Do This Month. Read inbox/ entries for the month plus the pasted Sunday context. Delegate mining, research, ideation, then creation, then edit. Output the 8-file folder cristo/$ARGUMENTS plus meta.json. Move the BOARD.md card to In Review PR when the PR is open. Never direct push to main.
